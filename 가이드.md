# 유동영도 쉽게 이해할수있는 사용설명서

Virtual StreamDeck 사용법이야. 천천히 따라오면 됨.

## 1. 이게 뭐하는 프로그램이야?

단축키 한 번 누르면 마우스 위치에 버튼판이 뜨고, 버튼을 누르면 미리 정해둔 동작(프로그램 실행, 단축키, 문자 입력 등등)이 실행되는 프로그램이야. 실물 스트림덱이랑 똑같은데 화면 안에 있는 거라고 보면 됨.

## 2. 설치하기

1. [릴리스 페이지](https://github.com/asfgsg1212-oss/virtual-streamdeck/releases/latest)에서 `Virtual-StreamDeck-Setup-x.x.x.exe` 다운로드
2. 실행하면 Windows가 "이 앱은 알 수 없는 게시자가 만들었어요" 같은 경고(SmartScreen)를 띄울 수 있음 → **"추가 정보"** 클릭 → **"실행"** 클릭
3. 설치 마법사 따라가면 끝

바이러스 아님. 서명 인증서가 없어서 나오는 경고일 뿐이야.

## 3. 켜고 기본으로 써보기

- 설치하면 화면 오른쪽 아래 트레이(시계 옆)에 아이콘이 생겨. 프로그램은 항상 여기 상주해있음.
- 기본 호출 단축키는 **Ctrl + Alt + Space**. 아무 데서나 이 키를 누르면 마우스 위치에 버튼판이 뜸.
- 버튼을 클릭하면 그 동작이 바로 실행돼.
- 마우스를 버튼판 밖으로 빼면 자동으로 닫혀. (이 "밖"의 범위는 설정에서 조절 가능 — 뒤에서 설명)
- 다시 닫고 싶으면: 마우스를 밖으로 빼거나, **Esc** 누르거나, 호출 단축키를 한 번 더 누르면 됨.

## 4. 버튼 만들고 편집하기

1. 트레이 아이콘 **더블클릭** (또는 우클릭 → "에디터 열기")
2. 빈 칸을 클릭하면 버튼 편집 창이 뜸
3. 라벨(이름), 아이콘(이미지나 그냥 배경색) 정하고, **동작**을 골라서 저장

## 5. 동작(버튼이 뭘 할지) 종류

| 동작 | 설명 |
|---|---|
| 단축키 | 키 조합을 그대로 눌러줌 (예: Ctrl+C). 여러 개 추가하면 순서대로 다 눌림 |
| 문자 입력 | 원하는 텍스트를 그대로 입력해줌 (클립보드로 붙여넣는 방식이라 한/영 상관없이 안 깨짐) |
| 프로그램 실행 | 지정한 프로그램을 켜줌. 여러 개 추가하면 한 번에 다 같이 켜짐 |
| URL 열기 | 브라우저로 주소를 열어줌. 여러 개 추가하면 한 번에 다 같이 열림 |
| 미디어 키 | 음소거, 볼륨, 재생/일시정지, 다음/이전 곡 |
| 마우스 클릭 | 버튼 누르기 직전 마우스 있던 위치에 클릭(좌/우/더블) |
| 페이지 전환 | 다른 페이지로 넘어가기 |
| 폴더 | 이 버튼 안에 또 다른 버튼판을 넣기 (하위 메뉴처럼) |
| **매크로 (여러 동작)** | 위에 있는 것들을 순서대로 여러 개 조합. 예: "프로그램 실행" 한 단계 + "URL 열기" 한 단계 → 버튼 하나로 둘 다 실행 |

여러 기능을 섞어서 쓰고 싶으면 **매크로**를 쓰면 됨. "+ 단계 추가"로 원하는 만큼 붙이기.

## 6. 페이지랑 프로필

- **페이지**: 버튼판 한 장. 사이드바에서 + 눌러서 추가, 연필 아이콘으로 이름 수정.
- **프로필**: 페이지들을 통째로 묶은 세트. 완전히 다른 용도(예: "게임용" / "작업용")로 나눌 때 씀.

## 7. 설정 (에디터 창 왼쪽 아래 ⚙ 설정)

- **호출 단축키**: 버튼판 띄우는 키 조합 바꾸기
- **Windows 시작 시 자동 실행**: 컴퓨터 켜면 자동으로 같이 켜짐
- **페이지 수 표시**: 버튼판 아래에 페이지 몇 개인지 점으로 표시할지
- **상시 고정 모드**: 8번 참고
- **덱 버튼 모양**: 버튼 크기/간격/모서리 둥글기/테두리 — 옆에 뜨는 미리보기 보면서 숫자 조절
- **마우스 벗어나면 자동으로 닫히는 범위**: 버튼판 바깥으로 이 정도 벗어나야 닫힌다는 여유 공간. 크기/투명도/선-면 표시 방식 조절 가능

## 8. 상시 고정 모드 (그냥 화면에 계속 띄워두고 싶을 때)

설정에서 "상시 고정 모드"를 켜면:
- 버튼 눌러도 안 닫히고 화면에 계속 떠 있음 (진짜 스트림덱처럼)
- 마우스가 밖으로 나가도 안 닫힘

**빠르게 켜고 끄기**: 버튼판 오른쪽 위에 📌 핀 아이콘이 있으면 그거 눌러서 바로 켜고 끌 수 있음 (설정에서 이 핀 아이콘 자체를 껐다 켰다 할 수도 있음).

**위치 옮기기**: 고정 모드일 때만 왼쪽 위에 ☰ 손잡이가 생겨. 그거 마우스로 잡고 끌면 원하는 자리로 옮길 수 있음.

## 9. 업데이트

새 버전이 나오면 프로그램 켤 때 "새 버전이 나왔습니다. 업데이트 하시겠습니까?" 하고 물어봐. "업데이트" 누르면 알아서 받고, 다 받으면 재시작할지 다시 물어봄.

수동으로 확인하고 싶으면 트레이 아이콘 우클릭 → **"업데이트 확인"**.

## 10. 자주 하는 실수 / 문제 해결

**Q. 단축키를 눌러도 버튼판이 안 떠요**
다른 프로그램이 그 키 조합을 이미 쓰고 있어서 그럴 수 있음. 설정에서 호출 단축키를 다른 조합으로 바꿔봐.

**Q. 버튼판이 안 사라져요**
상시 고정 모드가 켜져 있는 건 아닌지 확인. 켜져 있으면 오른쪽 위 📌 눌러서 끄거나 Esc 눌러.

**Q. 프로그램 경로를 어떻게 찾아요?**
직접 파일 찾지 말고, "프로그램 실행" 동작에서 **"프로그램 선택하기"** 누르면 지금 켜져 있는 프로그램 목록이 나와. 거기서 클릭하면 끝. 안 켜져 있는 프로그램은 "직접 찾아보기"로 exe 파일 찾아서 선택.

**Q. 문자 입력했는데 한글/영어가 이상하게 나와요**
구버전(0.2.0 이전) 얘기임. 최신 버전은 클립보드로 그대로 붙여넣는 방식이라 안 깨짐 — 업데이트부터 해봐.

**Q. 이거 바이러스 아니에요?**
아니야. 서명 인증서 없이 배포해서 Windows가 경고하는 것뿐. 소스코드 다 여기 있음: https://github.com/asfgsg1212-oss/virtual-streamdeck

---

이거 읽고도 모르겠으면 그건 유동영 잘못임.
